# 路线图与开发日志

> 记录规则(防腐烂):日志只留最近 3 个版本 + 决策(ADR),更早历史以 `git log` 为唯一档案;
> 版本号三处同步由 `tests/manifest.test.mjs` 机械强制。

## 里程碑与现状

- [x] M0 骨架 → M5 打磨(全部实现,静态校验全绿,v0.2.2)
- [x] **飞书文档长截图拼接实测通过**(2026-09-11,v0.2.1/0.2.2;面板诊断可用,
  首份真机 layoutMetrics 样本已归档 CDP 笔记并回灌测试)
- [x] **P001-P1 Agent 桥接已实现并本地验收通过**(v0.3.0;2026-09-11 用户确认)
- [x] **P002 MCP 直连已实现**(2026-09-11;扩展零改动不 bump,RELAY_VERSION 0.4.0;
  服务器端全链路测试过,待用户在 Cursor/Claude Code 冒烟)
- [ ] P3 候选:「扩展作为 Agent 的眼睛和手」(DOM/坐标/点击工具族,见后续候选)
- [ ] 剩余本地实测回归:`tests/fixtures/` 夹具页按验收表逐项过(M2–M5 覆盖面)

各里程碑验收标准:

| 里程碑 | 验收 |
|---|---|
| M0 | `chrome://extensions` 加载成功;CI 绿 |
| M1 | `Alt+Shift+V` 出图,预览可看可下载;BUSY/特权页提示正确 |
| M2 | `fixtures/lazy.html` 整页图 30/30 全亮;`clip-probe.html` 探针坐标目测正确 |
| M3 | `fixed.html` 长图无重复顶栏;`huge30k.html` 走分段且色带边界无缝 |
| M4 | 任意页面右键元素出精确图;`Alt+Shift+S` 框选、Esc 取消、截图里无遮罩 |
| M5 | `spa-inner-scroll.html` 有内部容器提示;无限流页有黄条提示 |

## 开发日志(最近 3 版)

- **v0.4.2(2026-09-14)整幅捕获「尾部回绕成首屏」修复:单边 16000 硬上限**
  (用户实测 17756px 飞书页 @dpr1、注记「已单张完整截取」但结尾显示首屏内容:
  面积闸 34M<60M 放行单张,**但 Chrome surface 捕获单边有 ~16384 设备 px 纹理上限,
  超限时不报错、尺寸/宽高比全对、尾部内容「回绕」成首屏复制**——几何校验防不住)。
  修复:`geom.MAX_CAPTURE_DIM=16000`(留安全边距)纳入 `pickEmulationScale`
  (面积+单边双约束,1× 都装不下返回 0 → 分段);兼容兜底路径按 scale×dpr 同样
  跳过超限档;元素条带路径加单边守卫。该页此后走 5 段捕获 → 预览自动合成为
  一张长图(分卷上限 30000 容纳),Agent 收 5 个 part 文件。夹具验收注释同步。
- **v0.4.1(2026-09-14)超长截图末段混入「头部内容」修复**(用户实测:拼最后一张时
  又把头部的图拼了上来)。根因:飞书类虚拟列表在仿真 resize/重渲染中会**漂移容器
  scrollTop**(最坏弹回 0),而分段循环只在滚动后立即读一次位置,渲染稳定等待的
  2~3 秒内漂移无人察觉——末段视口最矮、最易被弹回 0,拍到的就是头部。修复:
  **截图前复核并锁定滚动位置**:前跳(会丢内容)→ 本段作废重试;回缩(布局收缩)→
  按重测总高收缩段高并重仿真防重叠;整幅路径截图前重新归零;元素条带截图前重锁
  `rect.y`。服务器无法复现浏览器内漂移,需真机飞书文档复测。
- **v0.4.0(2026-09-14)P003 超长页治理:三层上限 + 分卷 + 桥多段损坏修复**
  (用户实测 17756px 飞书文档:Agent 端「长图丢了一大截」实为 v0.3.0 真 bug——
  relay 把多段 base64 直连成一个损坏文件,只能读出第一段;预览端黄条 5 连发 +
  逐段碎图则是治理缺失)。① 总长闸门 `maxTotalCssH=60000`:超出截断取前段并明示;
  ② 整幅优先:`geom.pickEmulationScale` 面积超上限先自动降尺度整幅(下限 1×),
  dpr=2 的该类页由此回到"单张完整图"路径;③ 单图上限 `maxPartDeviceH=12000`
  收敛分段段高;④ relay 按 `seg` 分组逐段落盘 `*_part i of N`,响应
  `paths/parts`(path 保留向后兼容,heightPx 多段不谎报),preview 分卷装箱
  (`planVolumes`,每卷 ≤30000 设备 px)每卷一张完整长图;⑤ 注记降噪:过程噪音
  合并为一条路径说明;新增夹具 `feishu-like.html`(20000px 内部容器)。
  relay/mcp/geom 三层服务器测试全链路补齐(双段落盘回归)。
- **2026-09-11 P002 MCP 直连实现**(扩展本体零改动 → 不 bump,仍 v0.3.0;
  RELAY_VERSION 0.3.0→0.4.0):`bridge/mcp.mjs` stdio JSON-RPC(内嵌 relay,端口占用
  自动降级客户端)+ 三工具(screenshot/health/tabs,仅回路径不内联图)+ `--install`
  一键安装(claude mcp add / Cursor 合并写 .bak 备份 / Codex 打印片段 / 自检)+
  `--dry-run`;`tests/mcp.test.mjs` 真子进程全链路(协议/截图落盘/降级/dry-run);
  指南新增「MCP 直连」章节。决策记录见 P002 §10。
  **用户本地冒烟反馈修复**:`--install` 遇 Cursor 自动创建的空 `mcp.json` 误判为
  「非法 JSON」拒写(且备份文案不实)→ 空文件视为无配置正常写入;非空非法才
  备份跳过;新增 install 非 dry-run 回归测试(空文件写入 + 合法 JSON 合并不丢已有
  server);测试环境加 `CLIPSHOT_SKIP_CLAUDE` 防真实改写 claude 配置。

- **2026-09-11 工程治理**(不 bump 版本):引入 `CLAUDE.md` 开发规范(AGENTS.md 为
  兼容指针)、项目级 neat-freak 收尾 skill(`.claude/skills/neat-freak/`)、版本一致性
  闸门(测试断言 manifest === EXT_VER === 日志条目);v0.1.x 日志移入 git 历史。

## 决策记录(ADR 摘要)

- **捕获引擎走 CDP 视口仿真(Emulation)主路径**:一次性全量渲染无逐屏拼接错位;
  `captureBeyondViewport` 经三轮实测证明不可靠(只作旧内核兜底),逐段仿真+preview
  堆叠仅作超大页兜底。调试横幅代价自用可接受。
- **可视区/框选/视口内元素不走 debugger**:captureVisibleTab + canvas 裁剪,完全无横幅。
- **classic SW + importScripts**:content script 无法 importScripts,manifest js 数组
  顺序是唯一零构建共享常量方案,SW 保持同风格。
- **元素拾取用 contextmenu 现场记录,否决 mouseover 路径回溯**:浏览器原生 hit-test 即
  目标,e.target 引用闭包保留;isConnected + 30s 新鲜度把竞态显式化为错误而非截错区域。
- **图片分块拉取(512KB)而非 data URL 单发/URL 携带**:几十 MB base64 在消息与 URL 通道都不可靠。
- **分段拼接在 preview 按各段实际解码高度堆叠**:规避非整数 dpr 的数学换算漂移。
- **sticky 用 visibility:hidden 而非 display:none**:sticky 占流空间,后者让正文回流
  改变长图版式;fixed 不在流中,display:none 安全。
- **框选时锁定滚动 + 捕获前隐藏遮罩**:前者保证视口坐标=选区坐标,后者防止把暗化遮罩截进图里。
- **文档治理:单一权威 + 机械闸门 + 每会话 neat-freak 收尾**:防腐烂不靠自觉,靠
  CI 断言与收尾规程(CLAUDE.md);文档言简意赅、分文件按需加载。

## 后续候选

iframe 内元素支持(all_frames + 坐标合成) · 截图前标注/画笔 · 滚动动画平滑
(GSAP 类页面逐帧等待) · `_locales` 正规化(若要发布) · PNG 压缩(zopflipng 离线) ·
截图历史列表页 · 飞书类页面复测反馈驱动的参数调优(渲染稳定超时/滚动速度) ·
**「扩展作为 Agent 的眼睛和手」**(用户 2026-09-11 构想,P3+ 再立项):把 DOM 快照/
元素坐标/点击执行也暴露成 MCP 工具,本地 Agent(Cursor/Claude Code)据此精准操作
浏览器,指哪打哪;不建议在扩展内嵌大模型(密钥/成本/更新负担),让宿主 Agent 当大脑。
