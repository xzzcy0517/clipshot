# 路线图与开发日志

> 记录规则(防腐烂):日志只留最近 3 个版本 + 决策(ADR),更早历史以 `git log` 为唯一档案;
> 版本号三处同步由 `tests/manifest.test.mjs` 机械强制。

## 里程碑与现状

- [x] M0 骨架 → M5 打磨(全部实现,静态校验全绿)
- [x] **飞书文档长截图拼接实测通过**(2026-09-11,v0.2.1/0.2.2;面板诊断可用,
  首份真机 layoutMetrics 样本已归档 CDP 笔记并回灌测试)
- [ ] **v0.4.5 超长飞书文档复测**:中段无重复、段缝连续(用户侧待验)
- [ ] **v0.8.9 飞书超长文档复测(P008)**:17693px 虚拟滚动页中段无重复、段缝连续;
  普通长页回归一致;无限流页收敛循环不挂死(用户侧待验)
- [ ] **v0.9.0 图片上传复测(P009 一期)**:popup 上传单/多图、列表各自编辑导出、
  下载格式/质量/缩放面板、空态上传入口(用户侧待验)
- [ ] **v0.7.2–0.7.4 UI/捕获批量复测**:面板置灰与悬停、框选无描边、
  看板内部容器页整幅底部完整(用户侧待验)
- [ ] 剩余本地实测回归:`tests/fixtures/` 夹具页按验收表逐项过(M2–M5 覆盖面)

各里程碑验收标准:

| 里程碑 | 验收 |
|---|---|
| M0 | `chrome://extensions` 加载成功;CI 绿 |
| M1 | 面板「可视区域截图」出图,预览可看可下载;BUSY/特权页提示正确 |
| M2 | `fixtures/lazy.html` 整页图 30/30 全亮;`clip-probe.html` 探针坐标目测正确 |
| M3 | `fixed.html` 长图无重复顶栏;`huge30k.html` 分段拍摄、预览合成单张且色带边界无缝 |
| M4 | 任意页面右键元素出精确图;面板「框选区域截图」拖拽、Esc 取消、截图里无遮罩 |
| M5 | `spa-inner-scroll.html` 有内部容器提示;无限流页有黄条提示 |

## 开发日志(最近 3 版)

- **v0.10.2(2026-09-20)修复:图片标注画出边界后导出缺失**(用户实测反馈):
  图片标注坐标存图像原始像素,拖出图像边缘时屏幕上可见(overlay 全视口)但导出
  画布按 natW×natH 裁剪,出界部分丢失(画板层标注因 bbox 并集不受影响)。
  修复口径:**导出画布 = 图像 ∪ 标注包围盒**(`P.unionRect`),`exportBlob` 升级为
  `exportRegion`(返回区域偏移 x/y,可为负);单图导出、多卡合成(`composeBoard`
  按偏移摆放:设备 px = card·dpr + 偏移·s)、画板 `boardBBox`(卡片视觉范围=
  图像∪标注)三处同数学;无标注仍原 blob 直通(零回归)。
- **v0.10.1(2026-09-20)P014:画板教学化增强 + 形状库**(用户逐条实测反馈):
  **缩放无级化**——滑杆(5%–400%)+ 数字输入框 + Ctrl+滚轮改指数平滑
  (`exp(-Δy·0.001)`,触控板逐像素),± 按钮降为 ×1.1 细调;**点空白消蓝框**
  (activeImg 仍作操作兜底);画板内 **＋图片 / Ctrl+V 粘贴**直接建卡(不走 SW
  重载),卡片悬停出 × 可移除(`Edit.unmount`);**画板标注层**——`#wrap` 注册为
  编辑器伪目标(isBoard,s=1),编辑态在**非图片区域**也能用全部工具写字/标注
  (马赛克除外,无像素可采样,toast 引导),导出 bbox=卡片∪画板标注(原点可为负,
  合成时 translate 对齐),标注层按 dpr 重绘进 PNG;**画板背景**透明(棋盘格)/白板/
  常用色/自定义取色,localStorage 记忆,非透明导出垫底色;**新建空白画布**=视口
  ×dpr 透明 PNG 卡片(非破坏追加);**形状库(快捷键 8)**:基本 8 + 线条 4(直线/
  箭头/双箭头/肘形)+ 流程图 5 + UML 5 + 图标 6,拖拽放置,bbox 类 8 把手、线条类
  端点拖拽,填充三态(无/淡/实),选中回显形状与填充;撤销/重做跨目标按操作 seq
  选最新者;顺带修复:**编辑态导出不再把选中虚线框画进 PNG**(导出视图加 export
  标志)。零回归口径不变:单卡+透明底+无画板标注仍原 blob 直通。
- **v0.10.0(2026-09-20)P013:预览页变画板**(minor,功能域扩到多图布局/拼图导出;
  用户拍板):图片即卡片**自由拖动摆位**,宽高只随缩放比走(**永不变形**);
  ≥2 张卡片下载/复制按画板布局 `composeBoard` 合成(含各卡标注)再接导出弹窗;
  **单卡零回归**(缩放为纯视图、无标注原 blob 逐字节直通);未手动拖过时按构建序
  自动纵向堆叠(分卷天然成序),用户一拖即停;编辑器零改动(标注读写实时 rect);
  合成超 32767 有 toast 引导缩小。
- **2026-09-11 工程治理**(不 bump 版本):引入 `CLAUDE.md` 开发规范(AGENTS.md 为
  兼容指针)、项目级 neat-freak 收尾 skill(`.claude/skills/neat-freak/`)、版本一致性
  闸门(测试断言 manifest === EXT_VER === 日志条目);v0.1.x 日志移入 git 历史。

## 决策记录(ADR 摘要)

- **捕获引擎走 CDP 视口仿真(Emulation)主路径**:一次性全量渲染无逐屏拼接错位;
  `captureBeyondViewport` 经三轮实测证明不可靠(只作旧内核兜底),逐段仿真+preview
  堆叠仅作超大页兜底。调试横幅代价自用可接受。
- **可视区/框选/视口内元素不走 debugger**:captureVisibleTab + canvas 裁剪,完全无横幅。
- **classic SW + importScripts**:content script 无法 importScripts,manifest js 数组
  顺序是唯一零构建共享常量方案,SW 保持同风格。
- **元素拾取用 contextmenu 现场记录,否决 mouseover 路径回溯**:浏览器原生 hit-test 即
  目标,e.target 引用闭包保留;isConnected + 30s 新鲜度把竞态显式化为错误而非截错区域。
- **图片分块拉取(512KB)而非 data URL 单发/URL 携带**:几十 MB base64 在消息与 URL 通道都不可靠。
- **分段拼接在 preview 按各段实际解码高度堆叠**:规避非整数 dpr 的数学换算漂移。
- **sticky 用 visibility:hidden 而非 display:none**:sticky 占流空间,后者让正文回流
  改变长图版式;fixed 不在流中,display:none 安全。
- **框选时锁定滚动 + 捕获前隐藏遮罩**:前者保证视口坐标=选区坐标,后者防止把暗化遮罩截进图里。
- **文档治理:单一权威 + 机械闸门 + 每会话 neat-freak 收尾**:防腐烂不靠自觉,靠
  CI 断言与收尾规程(CLAUDE.md);文档言简意赅、分文件按需加载。
- **超长页三原则(v0.4.0–0.4.5 六连实测蒸馏)**:① 仿真只允许原生 dpr(分数缩放
  大视口必撕裂);② 单边 16000 设备 px 是单张硬顶,超了只能分段,「人看的单张」
  由 preview 分卷合成承担;③ 虚拟列表页探测/滚动/拍摄(含预热)必须同一视口
  配置贯穿全程,逐段清除重建必产生重复缝。物理依据见 CDP 笔记。

## 后续候选

iframe 内元素支持(all_frames + 坐标合成) · 截图前标注/画笔 · 滚动动画平滑
(GSAP 类页面逐帧等待) · `_locales` 正规化(若要发布) · PNG 压缩(zopflipng 离线) ·
截图历史列表页 · 飞书类页面复测反馈驱动的参数调优(渲染稳定超时/滚动速度) ·
iframe 内截图支持(若有需要再提案)。
