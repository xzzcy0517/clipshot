# ClipShot 开发规范(唯一权威,AGENTS.md 指向本文件)

自用 Chrome MV3 扩展:整页滚动长截图 / 可视区 / 元素 / 框选。零构建零依赖原生 JS,
「加载已解压的扩展程序」直接使用。协议唯一真身:`common/messages.js`。

## 环境约束(最高优先级)

- 开发机无浏览器,**任何行为验证都依赖用户本地 Chrome 实测后反馈**,你只能自证静态正确。
- 门禁:`bash tests/run-all.sh`(语法 + manifest + geom 单测 + 协议防漂移 + 版本一致性
  + 深色覆写顺序),CI 同款。**commit 前必须全绿,没有例外。**
- 坐标系/捕获类疑难的排障回路:用户在问题页面点面板「诊断当前页面」→ 回传 JSON → 回填 `tests/geom.test.mjs` 与 `docs/CDP与坐标系笔记.md`。**没验证过的结论不写进权威文档,标 pending。**

## 纪律

0. **方案先行**(用户定死的开发模式):任何新功能/机制变更,先在 `docs/proposals/`
   写「P编号-标题.md」提案(头部状态表:草案→评审中→已批准→已实现/已否决),
   **与用户讨论确认后才动手写代码**;实现后回填状态与版本/commit。小修(bugfix、
   文案、测试)不强制走提案。
1. **单一权威**:每个事实只写一处(路由见下表),别处一行指针。禁止把同一机制复制进多个文件。
2. **版本同步**(测试锁死):`manifest.json version` === `CS.EXT_VER` === 开发日志 `vX.Y.Z` 条目。行为修复/功能=patch,捕获机制重构=minor;**版本号只在扩展本体变化时 bump**,纯文档/治理提交不 bump。
3. 错误文案只进 `ERR_TEXT`;UI 按码显示,不硬编码。新协议消息必须在两端文件都有引用(协议测试会拦)。
4. 提交:按主题分组、中文命令式标题。**每次代码修改完成后必须 commit 并 push 到
   GitHub**(开发在服务器端,用户在测试电脑 `git pull` 后刷新扩展卡片 ⟳ 验证)——
   不等用户要求,改完即推;推送失败要明说。
5. **Agent 桥接已于 v0.7.0 整体移除(P006)**:截图属调试用途,手动入口足够;
   勿复活 bridge/relay/mcp/skill/接管相关代码;确需自动化能力时走新提案。
6. **页面样式深色覆写**(`@media (prefers-color-scheme: dark)`)必须写在各 CSS 文件
   **末尾**:同优先级后者胜出,写前面会被浅色基础规则盖成「白底白字」(popup/options
   各栽一次)。`tests/css-dark.test.mjs` 机械拦截,别绕过它改顺序。

## 文档路由(按需读,勿全读)

| 任务 | 先读 |
|---|---|
| 新功能立项/查历史提案 | `docs/proposals/`(P编号-标题.md,状态表在头部) |
| 增改消息/协议 | `common/messages.js`(权威表)+ `docs/架构说明.md` |
| 捕获/CDP/坐标系问题 | `docs/CDP与坐标系笔记.md`(实测结论优先于一切文档描述) |
| 功能/UI 行为 | `docs/路线图与开发日志.md`(现状与验收)+ 对应页面源码 |
| 用户端使用问题 | `docs/安装与调试指南.md` |

## 会话收尾(提醒制,是否执行由用户决定)

一批工作完成或会话将结束**提醒一句**:「本轮是否执行洁癖收尾(/neat)?」——**只提醒,不擅自执行**。
用户确认后才运行 neat-freak(位于 `.claude/skills/neat-freak/`,轻量路径):对账代码↔文档↔规则、
把实测教训写入 CDP 笔记/开发日志、清点会话残留(删除候选**经确认后才删**)、文档与代码同批提交。
禁止代替用户判断是否收尾;发现长期未收尾时,新会话读到本条可再提醒一次。
